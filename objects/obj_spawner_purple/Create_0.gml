// Create Event of obj_spawner
spawn_interval = 1600; // Regular spawn interval for blobs
spawn_timer = 100 * room_speed; // Initialize the spawn timer to match the interval
total_time = 0; // Initialize total_time to track the elapsed time since creation
